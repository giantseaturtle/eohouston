User-agent: *
Allow: /

# The two /marketing-* pages are unlisted, but they are deliberately NOT disallowed here.
# They carry noindex via meta tag and X-Robots-Tag header, and Google has to be able to
# crawl a page to see that noindex. Blocking them here would leave the URLs indexable
# with no content, which is the opposite of what we want.

Sitemap: https://eohouston.com/sitemap.xml
